# 세상의 모든 밸런스 v3 제품 업그레이드 계획

문서 버전: v3.0  
작성일: 2026-07-14  
기반 문서: `balance_all_product_spec.md`  
리뷰 기준: `docs/superpowers/specs/2026-07-14-balance-game-product-review-design.md`

## 0. 업데이트 요약

v2.1의 아이디어를 폐기하지 않고 제품의 역할에 따라 재배치한다.

| 역할 | P0에서의 위치 | 핵심 기능 |
|---|---|---|
| 킬링타임 | 제품 본체 | 오늘의 질문, 무한 A/B 덱, 즉시 결과 |
| 자기 이해 | 축적 보상 | 8개 가치 축, 단계별 나의 뇌, 상위 두 축 조합 요약 |
| 질문 해결 | 콘텐츠 자급 | 통합 질문 작성, 테스트 배급, 공유 링크 |

예측 모드, 미니미, 레어 초이스, 친구 궁합과 월간 리와인드는 P0에서 제외한다. 가짜 투표와 손실형 스트릭은 구현하지 않는다.

## 1. 제품 정의

> **재미로 고르다 보면 나의 선택 지도가 자라고, 궁금한 것은 세상에 물어볼 수 있는 소셜 밸런스 게임.**

### 핵심 선순환

```text
킬링타임으로 선택
  → 선택 데이터가 나의 뇌에 축적
  → 사용자가 궁금한 질문을 생성
  → 질문이 다른 사용자의 덱 콘텐츠가 됨
  → 더 많은 선택과 질문이 쌓임
```

### 최우선 목표

첫 출시의 목표는 세션 길이가 아니라 **첫 방문자 중 7일 안에 3일 이상 투표한 사용자 비율**을 높이는 것이다.

## 2. 목표 사용자

- 핵심: 밸런스게임, MBTI, 짧은 콘텐츠와 SNS 공유에 익숙한 18~29세
- 확장: 설명 없이 A/B 선택을 즐길 수 있는 전 연령
- 콘텐츠 감도는 젊게 유지하고 조작과 문구는 전 연령이 이해하게 한다.

## 3. P0 사용자 경험

### 3.1 첫 방문

```text
앱 실행
  → 온보딩 없이 오늘의 질문 표시
  → 게스트로 A/B 선택
  → 전체 결과와 내 위치 확인
  → 다음 질문으로 이동
  → 5번째 선택 뒤 나의 뇌 성장 신호 표시
  → 10번째 선택 뒤 질문 작성 제안
```

가입은 첫 투표 앞에서 요구하지 않는다. 게스트의 선택은 기기에 저장하고 계정 생성 시 서버 계정으로 병합한다.

### 3.2 플레이

- 첫 카드는 매일 갱신되는 오늘의 대표 질문이다.
- 이후에는 사용자가 멈출 때까지 A/B 카드를 계속 제공한다.
- 카드에는 질문, 선택 A/B, 카테고리, 패스와 신고만 먼저 노출한다.
- 선택 후 0.8초 동안 비율과 `다수파·소수파·초접전` 피드백을 보여준다.
- 상세 정보와 이유 칩은 선택 이후에만 연다.
- 기본 덱에는 예측 단계를 넣지 않는다.

### 3.3 물어보기

필수 입력은 선택 A와 B뿐이다.

- 선택 A/B: 각각 최대 40자
- 상황 설명: 선택, 최대 120자
- 카테고리: 자동 제안 후 수정 가능
- 마감: 없음, 오늘, 직접 설정
- 공개 범위: 전체 공개, 링크 전용

`클래식`과 `지금 고민`을 별도 작성 시스템으로 분리하지 않는다. 마감 설정만으로 긴 수명 질문과 긴급 질문을 모두 표현한다.

### 3.4 나의 뇌

기본 가치 축은 자유, 안정, 관계, 현실, 감성, 성장, 효율, 재미의 8개다.

| 누적 선택 | 공개 내용 |
|---:|---|
| 1~9 | 뇌가 깨어나는 진행 표시 |
| 10~19 | 상위 가치 축 3개와 최근 변화 |
| 20~49 | 상위 두 가치 축의 조합 이름과 단순 연결 지도 |
| 50+ | 주제별 성향 차이와 주간 변화 |

모든 인사이트에는 영향을 준 선택 기록을 연결한다. 상위 두 축은 28개 조합을 모두 `자유·관계 선택가`와 같은 조합형 이름으로 표현하고, 일부 조합만 이름을 준 뒤 나머지를 하나의 기본 유형으로 합치지 않는다. 2위와 3위 점수가 같으면 `균형을 탐색하는 중`으로 표시한다. MBTI식 16유형으로 재구성하지 않으며, 성격을 영구적으로 단정하지 않고 `최근 선택에서 보인 경향`으로 표현한다.

### 3.5 친구·연인과 공유

- 공개 및 링크 전용 질문은 공유 URL을 제공한다.
- 링크 방문자는 가입 전에 한 번 투표하고 결과를 볼 수 있다.
- 링크 전용 질문은 공개 덱에 노출하지 않는다.
- 궁합 점수와 친구 비교는 P1에서 검증한다.

## 4. 정보 구조

하단 탭은 네 개만 사용한다.

1. **플레이:** 오늘의 질문과 무한 덱
2. **물어보기:** 질문 생성과 내 질문 결과
3. **나의 뇌:** 가치 축, 유형, 선택 근거
4. **마이:** 계정, 저장, 알림, 신고·차단과 데이터 삭제

검색, 랭킹과 카테고리 탐색은 P0 하단 탭에서 제외한다.

## 5. 콘텐츠 공급 계획

### 5.1 출시 전

- 상시 질문 300개를 작성하고 품질 기준으로 검수한다.
- 오늘의 대표 질문 14개를 미리 준비해 첫 2주 운영 공백을 없앤다.
- 질문마다 카테고리와 선택지별 가치 태그 1~2개를 지정한다.

### 5.2 출시 후

- 운영자는 오늘의 대표 질문 하루 1개에 집중한다.
- 사용자 질문은 작은 트래픽에서 먼저 시험한다.
- 패스율과 신고율이 낮고 선택률이 높은 질문을 넓게 배포한다.
- 중복 질문은 게시 전에 유사 질문을 보여주고 리믹스를 제안한다.
- 반응이 약한 질문은 작성자에게 문구 수정 또는 재게시 기회를 준다.

### 5.3 초기 덱 배합

| 콘텐츠 | 초기 비율 |
|---|---:|
| 검수된 상시 질문 | 50% |
| 검증된 사용자 질문 | 30% |
| 신규 사용자 질문 테스트 | 15% |
| 오늘의 질문·마감 임박 질문 | 5% |

비율은 기능 플래그로 조절하고 실제 패스율과 재방문 데이터를 기준으로 갱신한다.

### 5.4 배급 원칙

- 새 질문에는 실제 사용자 대상 최소 테스트 노출을 보장한다.
- 투표 수를 보장하거나 운영 계정으로 결과를 부풀리지 않는다.
- 동일 사용자가 같은 질문에 중복 투표하지 못하게 한다.
- 링크 전용 질문의 투표는 공개 덱 추천 점수에 반영하지 않는다.

## 6. 기능 우선순위

### P0 — 첫 제품 가설

- Expo 기반 iOS·Android·Web 공용 앱
- 게스트 세션과 선택 기록
- 오늘의 대표 질문
- 무한 A/B 덱, 패스, 즉시 결과와 햅틱
- 통합 질문 작성과 마감·공개 범위
- 신규 질문 테스트 배급
- 이유 칩, 신고와 차단
- 8개 가치 축 점수
- 단계별 나의 뇌와 상위 두 축 조합 요약
- 질문 공유 URL과 비회원 투표
- 첫 반응·마감 결과 알림
- 핵심 행동 이벤트 계측

### P1 — 선순환 강화

- 제한적 자유 댓글과 운영 도구
- 친구 선택 비교와 간단한 궁합
- 주간 나의 뇌 변화 카드
- 유형 공유 이미지
- AI 질문 다듬기와 중복 추천
- 카테고리 개인화
- 작성자 후일담

### P2 — 차별화 실험

- 주간 예측 챌린지
- 미니미 표현
- 레어 선택 컬렉션
- 월간 리와인드
- 그룹 전용 질문방
- 시즌·랭킹 이벤트

### 구현 금지

- 가짜 투표 또는 시드 계정으로 반응 부풀리기
- 기본 덱의 상시 예측 2단계
- 손실과 죄책감을 사용하는 스트릭
- 선택 근거가 없는 AI 성격 단정
- 신고·차단 없이 자유 댓글 공개

## 7. 기술 구조

### 클라이언트

- Expo SDK 57 기본 템플릿과 Node.js 22.13 이상
- React Native + TypeScript
- Expo Router
- TanStack Query: 서버 상태
- Zustand: 덱과 게스트 세션의 최소 로컬 상태
- React Native Reanimated: 카드 전환과 결과 오버레이
- Expo Haptics: 선택 피드백
- Expo Notifications: 첫 반응과 결과 알림

### 백엔드

- Supabase Auth, PostgreSQL, Row Level Security
- 데이터베이스 함수: 원자적 투표, 덱 후보 조회, 뇌 점수 집계
- Edge Function: 알림 전송과 배급 작업
- 모든 스키마 변경은 `supabase/migrations`로 관리한다.

### 테스트

- Jest + `jest-expo`
- React Native Testing Library
- Supabase CLI + pgTAP
- 핵심 라우트 E2E는 Maestro 시나리오로 검증한다.

## 8. 데이터 모델 v3

### 핵심 테이블

- `profiles`: 사용자 공개 프로필과 게스트 병합 정보
- `questions`: A/B 질문, 공개 범위, 마감과 단일 배급 단계 `stage`
- `question_options`: A/B 문구와 가치 태그 연결
- `votes`: 실제 선택과 투표 시점
- `question_skips`: 패스 기록
- `reason_reactions`: 구조화된 이유 칩
- `value_axes`: 8개 가치 축 사전
- `option_value_weights`: 선택지별 가치 축 가중치
- `user_value_scores`: 사용자별 누적 점수
- `daily_questions`: 날짜별 대표 질문
- `question_exposures`: 신규 질문의 실제 노출과 반응
- `reports`: 질문 신고
- `blocks`: 사용자 차단
- `push_tokens`: 알림 토큰과 동의 상태
- `analytics_events`: P0 핵심 행동 이벤트

### 핵심 제약

- `votes(question_id, user_id)`는 유일해야 한다.
- 오프라인 재전송은 `(user_id, client_action_id)`로 멱등 처리하며, 같은 동작의 재시도는 성공으로 간주한다.
- 선택 값은 해당 질문의 A 또는 B만 허용한다.
- 링크 전용 질문은 덱 조회 함수에서 제외한다.
- 마감된 질문은 새 투표를 받지 않는다.
- 질문의 공개·배급 상태는 `status`를 별도로 두지 않고 `stage = pending | test | active | limited | hidden`으로만 관리한다.
- 나의 뇌 점수는 실제 투표만 집계하고, 투표 삽입과 `user_value_scores` 갱신은 `cast_vote` 한 트랜잭션에서 처리한다. 클라이언트의 점수 직접 쓰기는 금지한다.
- 질문 삭제 시 개인 식별 정보는 제거하되 집계 보존 여부를 정책에 따라 명시한다.

## 9. 추천과 신규 질문 배급

### 9.1 초기 후보 구성

```text
candidate_score =
  quality_score * 0.40
  + freshness_score * 0.25
  + test_exposure_need * 0.25
  + close_deadline_score * 0.10
```

오늘의 질문은 점수 계산과 무관하게 첫 카드에 고정한다.
P0에서는 근거가 부족한 `category_match`를 점수에서 제외한다. 사용자의 유효 투표 이력이 쌓인 뒤 P1 실험으로만 추가한다.

### 9.2 품질 신호

- 선택률: 노출 후 A/B를 고른 비율
- 패스율: 노출 후 패스한 비율
- 신고율: 노출 대비 신고 비율
- 완료율: 카드 표시 후 결과까지 도달한 비율
- 반복 노출 방지: 사용자별 최근 노출 질문 제외

노출 10회 미만의 `test` 질문은 품질을 판정하지 않고 중립 사전값 `0.5`를 사용한다. 10회 이상부터 모든 비율의 분모를 `NULLIF(exposures, 0)`로 보호하고 `COALESCE`로 중립값을 적용한다. 신규 질문의 탐색 기회는 가짜 투표가 아니라 `test_exposure_need`로 보장한다.

### 9.3 배급 단계

```text
pending
  → test: 최소 테스트 노출
  → active: 품질 기준 통과
  → limited: 높은 패스율 또는 경계 품질
  → hidden: 정책 위반 또는 높은 신고율
```

## 10. 안전과 신뢰

- 특정 가능한 실명, 연락처, 학교와 회사 정보 입력을 차단한다.
- 미성년자 성적 내용, 혐오, 자해, 범죄와 개인정보 질문에 별도 정책을 적용한다.
- P0 자유 댓글은 제공하지 않고 이유 칩만 제공한다.
- 모든 신고는 저장하되 자동 배급 제한에는 일반 계정 또는 실제 투표 10회 이상인 익명 계정의 서로 다른 신고만 반영한다. 신뢰 조건을 충족하지 않은 익명 신고는 관리자 검토 대기열로만 보낸다.
- 신뢰 신고 가중치 합계 3 이상이면 `limited`로 낮추고, `hidden`은 관리자 판정으로만 전환한다. 신고는 `(question_id, reporter_id)`당 1회이며 요청 속도를 제한한다.
- 익명 가입에는 CAPTCHA 또는 Turnstile과 Supabase Auth의 IP 기반 속도 제한을 적용한다.
- 현재 저장소에는 site key/widget/token provider가 없어 운영 CAPTCHA 연동은 보류한다. 기존 유효 세션은 CAPTCHA 없이 재사용하고, `EXPO_PUBLIC_AUTH_CAPTCHA_REQUIRED=true`일 때 신규 익명 가입만 provider 부재 시 fail-closed한다. 운영 활성화 완료로 간주하려면 공급자 키 발급, 클라이언트 위젯 어댑터, Supabase secret 설정과 실기기 검증이 모두 필요하다.
- 사용자는 자신의 선택 기록, 유형 데이터와 계정을 삭제할 수 있다.
- 나의 뇌는 전문 심리 진단이 아니라 오락과 자기 성찰 도구라고 명시한다.

## 11. 계측 계획

### 필수 이벤트

- `app_opened`
- `question_impression`
- `question_voted`
- `question_skipped`
- `result_viewed`
- `brain_progress_viewed`
- `brain_type_unlocked`
- `question_create_started`
- `question_created`
- `question_shared`
- `shared_question_voted`
- `report_submitted`
- `notification_opened`

각 이벤트에는 익명 사용자 ID, 세션 ID, 질문 ID, 발생 시각과 진입 경로만 담고 질문의 민감한 자유 텍스트는 분석 속성으로 보내지 않는다.

## 12. 검증 게이트

### Gate 1 — 핵심 손맛

- 사용자 15명 중 12명 이상이 설명 없이 첫 투표 완료
- 사용자 15명 중 10명 이상이 자발적으로 10개 이상 선택

### Gate 2 — 콘텐츠 품질

- 사용자 질문의 중앙 패스율이 운영 질문보다 15%포인트 이상 나쁘지 않음
- 신고율 임계치를 넘은 질문이 자동으로 배급에서 제외됨

### Gate 3 — 재방문

- 오늘의 질문과 나의 뇌 집단의 `7일 내 3일 이상 투표율`이 기본 덱 대비 상대적으로 15% 이상 높음

### Gate 4 — 콘텐츠 자급

- 신규 질문의 90% 이상이 24시간 내 실제 투표 10개 이상 확보
- 첫 작성자의 14일 내 재작성률 20% 이상

### Gate 5 — 공유 유입

- 공유 링크 방문자의 투표 완료율 60% 이상
- 투표 완료자의 가입 전환율 15% 이상

## 13. 구현 단계

1. Expo 프로젝트·테스트·4탭 골격
2. 도메인 모델·300개 시드 형식·저장소 인터페이스
3. 오늘의 질문과 무한 덱의 로컬 수직 슬라이스
4. 통합 질문 작성과 링크 전용 질문
5. 가치 점수·단계별 나의 뇌와 유형
6. Supabase 스키마·RLS·원자적 투표 함수
7. 실제 덱 조회·신규 질문 테스트 배급
8. 이유 칩·신고·차단
9. 공유 링크·게스트 투표·익명 계정의 새 로그인 수단 연결
10. 알림·계측·보호 지표
11. 접근성·성능·오프라인 실패 복구
12. Gate 1 사용자 검증 후 다음 게이트 순차 진행

세부 파일, 인터페이스, 테스트와 커밋 단위는 `docs/superpowers/plans/2026-07-14-balance-game-mvp.md`를 따른다.

## 14. 최종 카피

메인 카피:

> **고르다 보면 내가 보이고, 궁금하면 세상에 물어본다.**

앱스토어 한 줄:

> **끝없이 즐기는 밸런스 게임, 선택으로 자라는 나의 뇌.**

상황별 카피:

- 킬링타임: `한 번만 고르려다 열 번 고르게 된다.`
- 자기 이해: `네 선택이 쌓이면, 네가 보인다.`
- 질문 작성: `둘이 갈렸다면 세상에 물어봐.`

## 15. 2026-07-14 구현 검증 상태

Task 1~12의 자동화 가능한 MVP 구현과 handoff 문서는 완료했다. Fresh local 검증에서 database
reset, pgTAP 268개 assertion, schema lint, Jest 194개 test, TypeScript, Expo Doctor 20/20과
Web/Android export가 통과했다. 실제 실행 명령과 운영 절차는 `mobile/README.md`를 따른다.

다음 항목은 구현 완료 또는 Gate 통과로 간주하지 않는다.

- Gate 1은 EAS test build, 실기기와 15명의 실제 참가자가 필요하며 현재 미실행·미판정이다.
- Maestro flow는 작성·정적 검증했지만 현재 개발 환경에는 Maestro와 연결된 device가 없어
  실기기 실행 결과가 없다.
- 44pt, 의미 구조, contrast와 고정 높이 clipping은 자동 검증하지만 200% 글자 크기,
  screen reader 순서, Reduce Motion과 작은 화면 겹침은 실기기 확인 전까지 미검증이다.
- `마이`의 선택 기록·유형 데이터·계정 삭제 UI와 운영 삭제 절차는 아직 placeholder이므로
  production launch blocker다.
- Production CAPTCHA는 provider key/widget/token adapter와 실기기 검증 전까지 미활성 상태다.
- APNs/FCM credentials와 production EAS project는 저장소 외부에서 구성해야 한다.

Gate 1의 두 기준을 모두 실제 관찰 데이터로 통과하고 개인정보 삭제 blocker를 해소하기 전에는
P1 기능 개발이나 production 출시를 시작하지 않는다.
