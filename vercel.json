{
  "buildCommand": "npm --prefix mobile ci && npm --prefix mobile run build",
  "outputDirectory": "mobile/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
