# Balance Island Agent Instructions

이 문서는 `balance-island` 프로젝트에서 작업하는 AI/개발 에이전트가 매 작업마다 따라야 하는 운영 지침이다.  
목표는 기능 구현 속도를 유지하면서도, 현재 프로젝트의 큰 리스크인 인코딩 손상, Supabase 보안/트랜잭션 경계, Expo Router 구조 누락을 반복해서 놓치지 않는 것이다.

## 1. 매 작업 시작 전 필수 루틴

1. `research.md`를 먼저 읽고 현재 알려진 구조와 리스크를 확인한다.
2. 작업 대상 파일을 직접 읽고, 기존 패턴과 현재 손상 상태를 확인한다.
3. 큰 코드 변경, DB 스키마 변경, 인증/권한 변경, 아키텍처 변경이 포함되면 작업 전 간단한 계획을 세운다.
4. 사용자 변경사항을 되돌리지 않는다. 의도치 않은 변경이나 충돌이 보이면 보존하면서 작업한다.
5. 한글 문자열을 다룰 때 UTF-8 인코딩 손상 여부를 특히 확인한다.

## 2. 우선순위

현재 프로젝트의 우선순위는 다음 순서다.

1. 컴파일을 막는 인코딩/문법 손상 복구.
2. Expo Router 기본 앱 셸 복구: `src/app/_layout.tsx`, `src/app/(tabs)/_layout.tsx`.
3. 타입체크 가능한 프로젝트 상태 만들기: lockfile, `tsconfig`, 앱 설정, 의존성 검증.
4. Supabase RLS, policy, auth profile 생성 trigger 정리.
5. 투표/리액션/질문 생성/게임화 갱신을 RPC 또는 Edge Function 트랜잭션으로 이동.
6. AI Edge Function 인증, rate limit, 입력 길이 제한, 임베딩 차원 검증.
7. 피드 pagination, 프리패치 검증, UI polish.

## 3. `research.md` 관리 규칙

`research.md`는 프로젝트 상태와 주요 리스크의 장기 기억이다.

다음 경우 반드시 `research.md`를 업데이트한다.

- DB 스키마, RLS, RPC, Edge Function 구조가 바뀐 경우.
- 피드, 투표, 리액션, 질문 생성, 게임화의 데이터 흐름이 바뀐 경우.
- 기존 Critical/Important 리스크를 해결했거나 새 리스크를 발견한 경우.
- 빌드/타입체크/테스트 결과로 기존 판단이 바뀐 경우.
- 프로젝트 구조에 큰 파일이 추가되거나 삭제된 경우.

업데이트 방식:

- 기존 내용을 무작정 덮어쓰지 말고, 새 사실과 해결 상태를 덧붙인다.
- 해결된 리스크는 삭제하기보다 “해결됨” 또는 “완화됨”으로 상태를 남긴다.
- 검증하지 않은 내용은 추정이라고 명확히 표시한다.

## 4. `timeline.md` 관리 규칙

`timeline.md`는 큰 작업의 변경 이력이다. 코드 변경이나 아키텍처 결정이 의미 있게 발생할 때마다 작업 시간 기준으로 로그를 남긴다.

로그를 남겨야 하는 경우:

- 새 기능 구현.
- 큰 버그 수정.
- DB 스키마/RPC/RLS/Edge Function 변경.
- 라우팅, 상태 관리, 서비스 레이어, 타입 정의 변경.
- 인코딩 복구 또는 대량 문자열 복구.
- 의존성 설치, 빌드 설정, 테스트 체계 변경.
- `research.md`의 핵심 결론 변경.

로그 형식:

```md
## YYYY-MM-DD HH:mm KST

- 작업: 무엇을 변경했는가.
- 범위: 영향을 받은 파일/모듈.
- 이유: 왜 변경했는가.
- 검증: 실행한 명령과 결과. 실행하지 못했으면 사유.
- 후속: 남은 위험 또는 다음 작업.
```

작은 오타 수정이나 주석 정리처럼 아키텍처/동작에 영향이 없는 변경은 생략해도 된다.

## 5. 코드 작성 원칙

- 기존 파일의 스타일과 패턴을 먼저 따른다.
- 새 추상화는 중복이나 복잡도를 실제로 줄일 때만 추가한다.
- 클라이언트에서 여러 테이블을 순차 갱신하는 로직은 가급적 서버 RPC/트랜잭션으로 이동한다.
- Supabase 클라이언트에서 민감한 쓰기를 직접 수행할 때는 RLS와 policy를 함께 검토한다.
- Edge Function이 비용을 발생시키면 인증, rate limit, 입력 제한을 함께 고려한다.
- `schema.sql`에 destructive reset이 필요하면 개발 전용임을 명확히 표시하고 운영 migration과 분리한다.
- 한글 UI/seed 데이터는 UTF-8로 저장하고, 깨진 문자열을 그대로 확산시키지 않는다.

## 6. 검증 원칙

작업 완료를 말하기 전에 가능한 검증을 먼저 실행한다.

권장 검증:

- TypeScript: `npx tsc --noEmit`
- Expo/React Native: 프로젝트 표준 실행 또는 Expo doctor
- SQL: Supabase 로컬 적용 또는 migration dry-run
- Edge Function: 로컬 serve 또는 최소 request smoke test
- 정적 확인: 변경 파일 직접 재읽기, 핵심 문자열/함수 검색

검증을 실행하지 못한 경우에는 이유를 명확히 남긴다. 예: 의존성 미설치, 네트워크 제한, 승인 거절, 로컬 Supabase 미구성.

## 7. 리뷰어 에이전트 사용 기준

큰 변경에는 읽기 전용 리뷰어 에이전트를 붙이는 것을 권장한다.

사용하면 좋은 경우:

- DB 스키마/RLS/RPC 변경.
- 투표/리액션/보상 같은 핵심 도메인 로직 변경.
- AI 파이프라인 또는 Edge Function 보안 변경.
- 여러 화면과 service/store/type이 함께 바뀌는 변경.
- 인코딩 복구처럼 대량 파일을 만지는 작업.

리뷰어에게는 “파일 수정 금지, 정적 분석 중심, 놓친 버그/계획상 위험 제보”를 명확히 지시한다.

## 8. 커뮤니케이션 규칙

- 작업 중에는 무엇을 읽고 있고 무엇을 발견했는지 짧게 공유한다.
- 큰 변경 전에는 어떤 파일을 왜 바꿀지 먼저 말한다.
- 완료 보고에는 변경 파일, 핵심 변경, 검증 결과, 남은 위험을 포함한다.
- 불확실한 내용은 확정처럼 말하지 않는다.
