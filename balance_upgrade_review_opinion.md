# 세상의 모든 밸런스 v3 통합 업그레이드 계획 검토 의견서

본 의견서는 `balance_all_upgrade_plan.md`와 `2026-07-14-balance-game-mvp.md` 문서를 분석하고, 구현 과정에서 발생할 수 있는 기술적 문제점, 기획적 불일치, 그리고 개선 방안을 정리한 문서입니다.

---

## 1. 종합 평가
전체적인 제품의 방향성(A/B 선택의 즉시성 + 데이터 축적을 통한 자기 이해 + UGC를 통한 콘텐츠 순환)은 매우 탄탄하게 설계되어 있습니다. 특히 이전 버전의 복잡한 보상 레이어(펫, RPG 등)를 걷어내고 **선택 피드, 나의 뇌, 질문 작성**의 3대 핵심 루프에 집중한 결정은 MVP 검증에 적절합니다.

그러나 **기획 문서(`balance_all_upgrade_plan.md`)와 구체적인 개발 스펙(`2026-07-14-balance-game-mvp.md`) 사이에 일부 용어 및 제약 조건의 불일치**가 존재하며, React Native/Expo 및 Supabase의 로컬 개발/테스트 환경에서 흔히 겪는 **실제 구현 장애 요인**들이 보입니다. 아래 세부 문제점과 개선 방안을 참고하여 구현 전에 계획을 보완하는 것을 권장합니다.

---

## 2. 핵심 문제점 및 개선 사항 (Issue Logs)

### 2.1. 데이터베이스 스키마와 RLS 정책의 용어 불일치
* **문제점:**
  * `2026-07-14-balance-game-mvp.md` Task 7의 RLS 정책 예시에서는 `status = 'active'` 컬럼 조건을 사용합니다.
    ```sql
    create policy "active public questions are readable"
    on public.questions for select
    using (visibility = 'public' and status = 'active' or author_id = auth.uid());
    ```
  * 그러나 동일 문서의 도메인 계약 및 Task 7 설명에서는 질문 배급 단계를 `stage` 컬럼 (`pending`, `test`, `active`, `limited`, `hidden`)으로 관리하도록 정의하고 있습니다.
  * 또한 `balance_all_upgrade_plan.md` 8장의 테이블 설명에는 `상태와 배급 단계`로 애매하게 표현되어 있어, 실제 마이그레이션 적용 시 `status`와 `stage`가 혼용되거나 테스트가 실패할 위험이 큽니다.
* **개선 방안:**
  * 컬럼명을 **`stage`**로 일원화하고, RLS 정책도 `stage = 'active'`로 수정해야 합니다.
  * `status`라는 중복 컬럼을 없애고, `stage`가 배급 단계와 활성화 상태를 모두 대변하도록 설계 통일이 필요합니다.

### 2.2. 로컬 에뮬레이터 환경의 Supabase 접속 설정 오류 (Android)
* **문제점:**
  * Task 12의 `.env.example`에는 `EXPO_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321`로 작성되어 있습니다.
  * iOS 시뮬레이터에서는 호스트의 localhost(`127.0.0.1`)에 정상 접근할 수 있으나, **Android 에뮬레이터는 자체 루프백을 사용하므로 `127.0.0.1`로 호스트 머신의 Supabase에 접속할 수 없습니다 (Connection Refused 발생).**
* **개선 방안:**
  * Android 에뮬레이터를 지원하기 위해 호스트 루프백 IP인 `http://10.0.2.2:54321`을 주석으로 명시하고 개발자 가이드에 추가해야 합니다.
  * 예시:
    ```bash
    # iOS Simulator & Web
    EXPO_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321
    # Android Emulator
    # EXPO_PUBLIC_SUPABASE_URL=http://10.0.2.2:54321
    ```

### 2.3. 신규 질문(UGC) 배급 점수 계산에서의 예외 (Cold Start & Division by Zero)
* **문제점:**
  * Task 8에서 피드 추천 시 아래 수식을 SQL RPC로 구현하려 합니다.
    ```sql
    candidate_score = quality_score * 0.35 + freshness_score * 0.25 + ...
    ```
  * 이때 신규 질문은 투표나 노출 수(exposures)가 `0`이므로, `quality_score`(선택률, 패스율 등)를 계산할 때 **0으로 나누기(Division by Zero)**가 발생하거나 `NULL`이 반환되어 정렬에서 완전히 배제될 수 있습니다.
  * 또한 `category_match`는 P0 단계에서 관심사 기반 개인화가 비활성(P1) 상태이므로 가중치만 있고 실제 연산에 쓰일 사용자 데이터가 모호합니다.
* **개선 방안:**
  * `exposures`가 일정 수치(예: 10회) 미만인 테스트 단계(`stage = 'test'`)의 질문은 `quality_score`에 디폴트 값(예: `0.5` 또는 `1.0`)을 강제 부여하는 예외 처리를 SQL RPC(`get_feed`) 내에 설계해야 합니다.
  * P0의 `category_match`는 사용자의 최근 투표가 많은 카테고리를 기준으로 가볍게 매칭하거나, 고정값 `0`을 반환하여 수식을 유지하되 오버헤드를 줄여야 합니다.

### 2.4. "나의 뇌" 성향(Archetype) 분석 매핑의 한계
* **문제점:**
  * Task 6에서는 8개 가치 축 중 상위 2개를 추출해 성향 아키타입(Archetype)을 부여합니다.
  * 8개 축 중 2개를 순서 없이 고르는 조합은 총 28가지(${}_8\mathrm{C}_2$)입니다.
  * 하지만 정의된 매핑은 단 4가지(`자유로운 탐험가`, `다정한 안정파`, `현실적인 해결사`, `감성적인 성장가`)뿐이며, **나머지 24가지 조합은 전부 `균형을 찾는 선택가`로 뭉뚱그려집니다.**
  * 결과적으로 85% 이상의 사용자가 동일한 결과(`균형을 찾는 선택가`)를 받게 되어, 사용자에게 개인화된 인사이트라는 느낌을 주지 못하고 리텐션(재방문) 동기가 약화될 우려가 있습니다.
* **개선 방안:**
  * **대안 A (간단한 극성 차원 MBTI화):** 8가지 축을 MBTI처럼 서로 대립하는 4가지 극성 차원으로 재구성하여 16가지 성향 코드로 매핑합니다.
    * 예: 자유(Freedom) vs 안정(Stability), 현실(Reality) vs 감성(Emotion), 효율(Efficiency) vs 관계(Relationship), 성장(Growth) vs 재미(Fun)
    * 각 극성의 점수 차를 구하여 4글자 유형(예: F-R-E-G 등)으로 만들면 16개의 성향을 직관적이고 촘촘하게 표현할 수 있습니다.
  * **대안 B (조합 보완):** 최소 12~14개 이상의 주요 조합에 고유 아키타입을 부여하고, 매핑되지 않은 극단적인 예외 조합에 한해서만 `균형을 찾는 선택가`를 제공합니다.

### 2.5. 익명 게스트 차단의 어뷰징 취약점 (Sybil Attack)
* **문제점:**
  * Task 9에서 사용자의 질문 신고(Report)가 3회 누적되면 해당 질문을 자동으로 `limited` 상태로 격하시켜 노출을 중단합니다.
  * 그러나 이 서비스는 게스트 상태(익명 인증)로 모든 기능을 바로 사용할 수 있습니다. 악의적인 사용자가 기기 초기화나 익명 계정 재발급을 반복하여 **경쟁 질문에 허위 신고를 3회 날려 즉각적으로 콘텐츠를 매장(Censor)시키는 어뷰징**이 매우 쉽게 발생할 수 있습니다.
* **개선 방안:**
  * 자동 격하(`limited` 전환)를 트리거하는 신고는 **일반 회원가입 계정(Email/Social) 또는 누적 투표가 10회 이상인 신뢰할 수 있는 게스트 계정의 신고만 가중치를 두어 합산**하도록 필터링을 걸어야 합니다.
  * 단순 게스트의 신고는 임계치를 10회 이상으로 높이거나, 관리자 대시보드 검수 대기열로만 보내고 자동 숨김은 처리하지 않도록 RLS/RPC 단에서 보완해야 합니다.

### 2.6. Expo SDK 57 프로젝트 생성 및 종속성 명령어 이슈
* **문제점:**
  * Task 1 Step 1의 생성 명령어: `npx create-expo-app@latest mobile --template default@sdk-57`
  * `--template` 뒤의 `@sdk-57` 문법은 `create-expo-app`의 공식 템플릿 탐색 알고리즘에서 오류를 내거나 최신 SDK 58로 프로젝트를 생성할 위험이 있습니다.
* **개선 방안:**
  * SDK 57을 보장하기 위해 다음과 같이 명령어를 수정하는 것이 안전합니다.
    ```powershell
    npx create-expo-app@57 mobile
    # 또는 명시적인 공식 템플릿 사용
    npx create-expo-app mobile --template blank
    ```

### 2.7. 투표 데이터와 가치 점수(`user_value_scores`)의 갱신 시점
* **문제점:**
  * 사용자가 투표를 할 때마다 가치관 점수가 실시간으로 업데이트되어야 `나의 뇌` 탭에서 반영된 애니메이션을 바로 볼 수 있습니다.
  * 투표 시 클라이언트가 직접 `user_value_scores`를 계산하여 백엔드에 업데이트하는 것은 보안 및 데이터 무결성 측면에서 금지되어야 합니다.
* **개선 방안:**
  * Supabase의 `votes` 테이블에 `AFTER INSERT` 트리거를 설정해야 합니다.
  * 사용자가 투표를 던지면, DB 단에서 트리거가 작동하여 `option_value_weights`를 읽고 해당 사용자의 `user_value_scores`에 점수를 자동으로 더해주는 방식(Atomic upsert trigger)을 마이그레이션 `202607140002_feed_and_vote_functions.sql`에 명시하는 것을 권장합니다.

---

## 3. 권장 구현 로드맵 보완
계획서의 12단계 중 특히 **Task 4 (오프라인 큐) → Task 7, 8 (데이터베이스 및 RLS) → Task 10 (계정 continuity)**로 이어지는 구간은 데이터 싱크 및 세션 유지가 복잡하게 얽히는 지점입니다. 

개발자는 다음 우선순위로 디버깅 포인트를 마련해야 합니다.
1. **SQLite 큐 트랜잭션 보장:** 게스트 세션에서 생성된 오프라인 큐가 온라인 전환 후 Supabase에 삽입될 때, `cast_vote` RPC 내에서 이미 동일 유저가 투표한 이력이 있다면 예외 처리(Ignore)하고 큐에서 안전하게 소거하는 로직을 확실하게 심어야 합니다.
2. **익명 계정 병합 로직:** 일반 로그인 계정으로 업그레이드할 때 Supabase Auth의 `linkIdentity`나 백엔드 커스텀 RPC를 통해 기존 `anonymous user_id`의 투표/뇌 점수 레코드를 새 `auth.uid()`로 `UPDATE` 해주는 계정 통합 마이그레이션 트리거를 마련해야 데이터 유실을 막을 수 있습니다.

---

## 4. 의견 요약 및 결론
본 업그레이드 계획은 매우 정교하며 훌륭한 사용자 시나리오를 담고 있습니다. 
위에 지적한 **1) RLS 정책 내 컬럼명 일원화(`stage`), 2) Android 에뮬레이터 접속 IP 가이드 추가, 3) 0 나누기 예외 처리, 4) 가치 성향 아키타입(MBTI화) 다변화, 5) 익명 어뷰징 방지 필터링**을 구현 세부 단계에 반영한다면, 개발 과정에서의 병목을 80% 이상 제거하고 론칭 후 안정적인 지표 모니터링이 가능할 것입니다.
